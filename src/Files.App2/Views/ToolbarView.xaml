<!--  Copyright (c) Files Community. Licensed under the MIT License.  -->
<UserControl
	x:Class="Files.App2.Views.ToolbarView"
	x:Name="Root"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<Grid
		Padding="8,4"
		ColumnSpacing="6"
		Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>

		<TextBlock
			VerticalAlignment="Center"
			FontWeight="SemiBold"
			Text="{x:Bind ViewModel.ActiveTab.Title, Mode=OneWay}" />
		<Button
			Grid.Column="1"
			Command="{x:Bind ViewModel.NewPaneCommand.Command}"
			ToolTipService.ToolTip="New pane"
			AutomationProperties.Name="New pane">
			<SymbolIcon Symbol="Add" />
		</Button>
		<Button
			Grid.Column="2"
			Command="{x:Bind ViewModel.ClosePaneCommand.Command}"
			IsEnabled="{x:Bind ViewModel.ClosePaneCommand.IsEnabled, Mode=OneWay}"
			ToolTipService.ToolTip="Close pane"
			AutomationProperties.Name="Close pane">
			<SymbolIcon Symbol="Cancel" />
		</Button>
	</Grid>
</UserControl>
