<!--  Copyright (c) Files Community. Licensed under the MIT License.  -->
<UserControl
	x:Class="Files.App2.Views.ToolbarView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	x:Name="Root">
	<Grid
		Padding="8,4"
		Background="{ThemeResource CardBackgroundFillColorSecondary}"
		BackgroundSizing="InnerBorderEdge"
		BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
		BorderThickness="1"
		ColumnSpacing="6"
		CornerRadius="8">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>

		<TextBlock
			VerticalAlignment="Center"
			FontWeight="SemiBold"
			Text="{x:Bind ViewModel.ActiveTab.Title, Mode=OneWay}" />
		<Button
			x:Uid="NewPaneButton"
			Grid.Column="1"
			Command="{x:Bind ViewModel.NewPaneCommand.Command}">
			<SymbolIcon Symbol="Add" />
		</Button>
		<Button
			x:Uid="ClosePaneButton"
			Grid.Column="2"
			Command="{x:Bind ViewModel.ClosePaneCommand.Command}"
			IsEnabled="{x:Bind ViewModel.ClosePaneCommand.IsEnabled, Mode=OneWay}">
			<SymbolIcon Symbol="Cancel" />
		</Button>
	</Grid>
</UserControl>
