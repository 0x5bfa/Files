<!--  Copyright (c) Files Community. Licensed under the MIT License.  -->
<Page
	x:Class="Files.App2.Views.RootView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:views="using:Files.App2.Views"
	x:Name="Root">
	<Grid RowSpacing="1">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<views:TabView x:Name="TabStrip" ViewModel="{x:Bind ViewModel, Mode=OneWay}" />

		<views:NavigationToolbar Grid.Row="1" ViewModel="{x:Bind ViewModel, Mode=OneWay}" />

		<NavigationView
			x:Name="Sidebar"
			Grid.Row="2"
			IsBackButtonVisible="Collapsed"
			IsSettingsVisible="False"
			PaneDisplayMode="LeftCompact"
			SelectionChanged="NavigationView_SelectionChanged">
			<NavigationView.MenuItems>
				<NavigationViewItem
					x:Name="HomeItem"
					Content="Home"
					Tag="Home">
					<NavigationViewItem.Icon>
						<SymbolIcon Symbol="Home" />
					</NavigationViewItem.Icon>
				</NavigationViewItem>
			</NavigationView.MenuItems>
			<NavigationView.Content>
				<Grid RowSpacing="1">
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="*" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>

					<views:ToolbarView ViewModel="{x:Bind ViewModel, Mode=OneWay}" />

					<views:PaneHost Grid.Row="1" ViewModel="{x:Bind ViewModel.ActiveTab, Mode=OneWay}" />

					<views:TerminalView Grid.Row="2" Visibility="Collapsed" />

					<views:InfoPane Grid.Row="3" Visibility="Collapsed" />

					<Border
						Grid.Row="4"
						Padding="12,6"
						Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
						<TextBlock
							Opacity="0.7"
							Text="{x:Bind ViewModel.StatusText, Mode=OneWay}"
							TextTrimming="CharacterEllipsis" />
					</Border>
				</Grid>
			</NavigationView.Content>
		</NavigationView>
	</Grid>
</Page>
